package drake;

// Stub message for outputting debug information from plan eval.
struct lcmt_plan_eval_debug_info {
  int64_t timestamp;

  lcmt_dof_keyframe dof_motion;

  // Planned body pose, vel, acc.
  int32_t num_body_motions;
  string body_motion_names[num_body_motions];
  lcmt_cartesian_keyframe body_motions[num_body_motions];

  // Contact related stuff.
  // TODO.
  double nominal_wrench[6];
}
